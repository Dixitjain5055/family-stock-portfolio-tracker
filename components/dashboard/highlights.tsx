import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { formatCurrency, formatPercent } from "@/lib/utils";
import type { AggregatedHolding } from "@/types/portfolio";
function List({title,rows,kind}:{title:string;rows:AggregatedHolding[];kind:"weight"|"move"}){const total=rows.reduce((s,r)=>s+(r.currentValue??r.invested),0);return <Card><CardHeader><h3 className="font-bold">{title}</h3></CardHeader><CardContent><div className="space-y-3">{rows.length?rows.map((row,index)=><div key={`${row.assetType}:${row.ticker}`} className="flex items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-[var(--surface-2)] text-xs font-bold">{index+1}</span><div className="min-w-0"><p className="truncate text-sm font-bold">{row.assetType==="mutual_fund"?row.companyName:row.ticker}</p><p className="truncate text-[10px] text-[var(--muted)]">{row.assetType==="mutual_fund"?`${row.ticker} · `:""}{row.owners.length} owner{row.owners.length===1?"":"s"}</p></div></div><div className="shrink-0 text-right text-sm font-semibold">{kind==="weight"?<><p>{formatCurrency(row.currentValue??row.invested)}</p><p className="text-[10px] text-[var(--muted)]">{total?(((row.currentValue??row.invested)/total)*100).toFixed(1):0}% shown value</p></>:<p className={(row.dayChangePercent??0)>=0?"text-emerald-700":"text-red-700"}>{(row.dayChangePercent??0)>=0?<ArrowUpRight className="inline" size={14}/>:<ArrowDownRight className="inline" size={14}/>} {formatPercent(row.dayChangePercent)}</p>}</div></div>):<p className="py-7 text-center text-sm text-[var(--muted)]">No market movement available.</p>}</div></CardContent></Card>}
export function Highlights({holdings}:{holdings:AggregatedHolding[]}){const moved=holdings.filter(h=>h.dayChangePercent!=null);return <div className="grid gap-4 xl:grid-cols-3"><List title="Top holdings" rows={[...holdings].sort((a,b)=>(b.currentValue??b.invested)-(a.currentValue??a.invested)).slice(0,5)} kind="weight"/><List title="Today’s gainers" rows={[...moved].filter(h=>(h.dayChangePercent??0)>0).sort((a,b)=>(b.dayChangePercent??0)-(a.dayChangePercent??0)).slice(0,5)} kind="move"/><List title="Today’s losers" rows={[...moved].filter(h=>(h.dayChangePercent??0)<0).sort((a,b)=>(a.dayChangePercent??0)-(b.dayChangePercent??0)).slice(0,5)} kind="move"/></div>}
