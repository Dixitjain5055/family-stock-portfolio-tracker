import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { formatCurrency, formatNumber } from "@/lib/utils";
import type { ExitedTrade } from "@/types/portfolio";
export function TradeHistory({trades}:{trades:ExitedTrade[]}){return <Card><CardHeader><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold">Realized P/L & history</h2><p className="mt-1 text-xs text-[var(--muted)]">Complete partial and full exit log</p></div><span className="rounded-full bg-[var(--surface-2)] px-3 py-1 text-xs font-semibold">{trades.length} trades</span></div></CardHeader><CardContent>{trades.length?<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="text-xs uppercase text-[var(--muted)]"><tr>{["Member","Ticker","Sale date","Quantity","Proceeds","Cost","Fees","Realized P/L","Tax estimate"].map(h=><th className="py-3 pr-4" key={h}>{h}</th>)}</tr></thead><tbody>{trades.map(t=><tr key={t.id} className="border-t"><td className="py-3 pr-4 font-semibold">{t.family_members?.name??"Member"}</td><td className="pr-4 font-bold">{t.ticker}</td><td className="pr-4 text-xs">{new Date(t.sell_date).toLocaleDateString("en-IN")}</td><td className="pr-4">{formatNumber(t.quantity)}</td><td className="pr-4">{formatCurrency(t.sell_price*t.quantity)}</td><td className="pr-4">{formatCurrency(t.buy_price*t.quantity)}</td><td className="pr-4">{formatCurrency(t.fees)}</td><td className={`pr-4 font-bold ${t.realized_pl>=0?"text-emerald-700":"text-red-700"}`}>{formatCurrency(t.realized_pl)}</td><td><span className="whitespace-nowrap rounded-full bg-[var(--surface-2)] px-2 py-1 text-xs">{t.tax_tag}*</span></td></tr>)}</tbody></table><p className="mt-3 text-[10px] text-[var(--muted)]">* User-selected holding-period estimate; not tax advice.</p></div>:<div className="py-10 text-center text-sm text-[var(--muted)]">Completed sales will appear here with cost, fees, and estimated tax tags.</div>}</CardContent></Card>}
